const AccountBox = () => {
  return (
    <div>
      {/* {' '}
      <p>Total IGC: </p>
      {igcBalance && <p>{igcBalance}</p>}
      <p>
        Account:{' '}
        {accounts
          .map((account) => `${account.slice(0, 7)}...${account.slice(-4)}`)
          .join(', ')}
      </p>{' '} */}
    </div>
  );
};

export default AccountBox;
